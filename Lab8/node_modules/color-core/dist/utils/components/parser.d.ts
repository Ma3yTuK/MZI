import { AdobeRGB, CIExyY, CMYK, HPLuv, HSI, HSL, HSLuv, HSV, HWB, LAB, LCH, LUV, Oklab, Oklch, RGB, SRGB, XYZ, YUV } from '../../types';
export declare function parseColor(input: string): RGB | HSL | HSV | CMYK | LAB | LCH | XYZ | YUV | Oklab | Oklch | HSLuv | HPLuv | CIExyY | LUV | SRGB | HSI | HWB | AdobeRGB | null;
declare const parseValue: (value: string, max: number) => number;
declare const parseAngle: (angle: string) => number;
export { parseAngle, parseValue };
