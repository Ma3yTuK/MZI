import { Color } from '../../color';
/**
 * Converts a color to grayscale.
 * @param color - The color to convert.
 * @returns A new Color instance in grayscale.
 */
export declare function grayscale(color: Color): Color;
