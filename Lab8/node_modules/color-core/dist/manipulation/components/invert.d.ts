import { Color } from '../../color';
/**
 * Inverts a color.
 * @param color - The color to invert.
 * @returns A new Color instance with inverted RGB values.
 */
export declare function invert(color: Color): Color;
